class Camera{
    constructor(src='Não informado', tracking_vagas=[], status=False){
        this.src = src,
        this.tracking_vagas = tracking_vagas
        this.status = status
    }
}

export default Camera